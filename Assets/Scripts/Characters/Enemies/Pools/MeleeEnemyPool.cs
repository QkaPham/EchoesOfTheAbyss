using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MeleeEnemyPool : BasePool<BasePoolableEnemy>
{
    //Player player;
    //FragmentPool fragmentPool;
    //ItemPool itemPool;
    //protected override BasePoolableEnemy Create()
    //{
    //    var meleeEnemy = Instantiate(Prefabs, this.transform).GetComponent<MeleeEnemy>();
    //    meleeEnemy.Init(player, fragmentPool.pool, Vector3.zero, itemPool.pool);
    //    meleeEnemy.SetPool(pool);
    //    return meleeEnemy;
    //}
}
